---
status: accepted
---

# Serve curated music outside the offline shell

OpenFray serves an approved music catalog from its own origin and does not integrate streaming providers or arbitrary media URLs. Tracks load on demand and remain outside the required offline shell because eager audio precaching would increase installation cost and weaken reliable offline reopening. An encounter may persist a stable track ID, while playback state stays in device runtime. Unavailable audio never blocks combat.
